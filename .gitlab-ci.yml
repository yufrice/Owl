image: yufrice/yesod-hlint

stages:
  - test
  - deploy

cache:
    paths:
    - .stack-work
    - .stack-test

test:
  stage: test
  allow_failure: true
  script:
    - "stack test --flag Owl:library-only --flag Owl:dev --install-ghc"

test-lint:
  stage: test
  allow_failure: true
  script:
    - "hlint ."

deploy:
  stage: deploy
  script:
    - "yesod keter"
  artifacts:
    paths:
      - Owl.keter
  only:
    - tags
